/*
Да се напише програма во која се вчитува матрица од цели броеви A со N редици и N * 2 колони (N не е поголемо од 50). Програмата треба да ја трансформира
вчитаната матрица во нова матрица B, така што сите елементи десно од N-тата колона ќе ги префрли под N-тата редица, односно од матрица со димензија
N редици и 2 * N колони ќе се добие матрица со 2 * N редици x N колони. 
*/
#include <iostream>
using namespace std;

int main() {
    int N;
    cin >> N; // Читање на бројот N

    // Матрица A има N редици и 2N колони
    int A[N][2 * N];

    // Вчитување на елементите во матрицата A
    for (int i = 0; i < N; i++) {
        for (int j = 0; j < 2 * N; j++) {
            cin >> A[i][j];
        }
    }

    // Матрица B има 2N редици и N колони
    int B[2 * N][N];

    // Првите N колони од A ги ставаме во првите N редици од B
    for (int i = 0; i < N; i++) {
        for (int j = 0; j < N; j++) {
            B[i][j] = A[i][j];
        }
    }

    // Вторите N колони од A ги ставаме во следните N редици од B
    for (int i = 0; i < N; i++) {
        for (int j = 0; j < N; j++) {
            B[i + N][j] = A[i][j + N];
        }
    }

    // Печатење на матрицата B (2N редици, N колони)
    for (int i = 0; i < 2 * N; i++) {
        for (int j = 0; j < N; j++) {
            cout << B[i][j] << " ";
        }
        cout << endl;
    }

    return 0;
}
/*
Префрлање на левата половина од A во горниот дел на B:
for (int i = 0; i < N; i++) {
    for (int j = 0; j < N; j++) {
        B[i][j] = A[i][j];
    }
}


📌 Тука ги земаме првите N колони од A и ги сместуваме во првите N редици на B.

Префрлање на десната половина од A во долниот дел на B:
for (int i = 0; i < N; i++) {
    for (int j = 0; j < N; j++) {
        B[i + N][j] = A[i][j + N];
    }
}


📌 Тука ги земаме вторите N колони од A (A[i][j + N]) и ги сместуваме во следните N редици на B (i + N).

Печатење на резултатот:
for (int i = 0; i < 2 * N; i++) {
    for (int j = 0; j < N; j++) {
        cout << B[i][j] << " ";
    }
    cout << endl;
}


📌 Ја печатиме матрицата B, редица по редица.

🔹 Визуелно претставување (N=3):

Матрица A (3 x 6):

a11 a12 a13 a14 a15 a16
a21 a22 a23 a24 a25 a26
a31 a32 a33 a34 a35 a36


Матрица B (6 x 3):

a11 a12 a13   ← лев дел од 1-ва редица на A
a21 a22 a23   ← лев дел од 2-ра редица на A
a31 a32 a33   ← лев дел од 3-та редица на A
a14 a15 a16   ← десен дел од 1-ва редица на A
a24 a25 a26   ← десен дел од 2-ра редица на A
a34 a35 a36   ← десен дел од 3-та редица на A

🔹 Заклучок:

Програмата ја преуредува матрицата така што од една „широка“ матрица N x 2N добиваме „висока“ матрица 2N x N, со тоа што десниот дел од A се префрла под левиот дел.



*/
